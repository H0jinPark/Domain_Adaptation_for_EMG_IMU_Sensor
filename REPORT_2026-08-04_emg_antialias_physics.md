# 2026-08-04 실험 보고서

**실험 1** EMG 안티에일리어싱 A/B · **실험 2** Physics-informed CDAN (EMG→IMU 디코더)

| 항목 | 값 |
|---|---|
| 러너 | `EMG/run_emg_antialias_2026-08-04.sh` → `EMG_physics/run_physics_2026-08-04.sh` (체인) |
| 실행 | 2026-08-04 16:32 → 20:39 (34분 + 211분) |
| 잡 | **10완료 / 0스킵 / 0실패** (2 + 8) |
| seed | 0, 1, 2, 3, 4 (전 잡 공통) |
| 백본 | 원본 크기 (compact 아님) |
| 로그 | `logs/emg_antialias_2026-08-04/driver.log`, `logs/physics_2026-08-04/driver.log`, `logs/chain_2026-08-04.log` |
| 결과 | `results/EMG/emg_baseline_result_{aaoff,aa}.json`, `results/EMG_physics/phys_cdan_result_*.json` |

두 스윕은 `flock` 으로 직렬화했고 체인 로그에 락 대기(16:53) → 해제 확인(17:07) 이 남아 있다.
driver.log 의 START 가 잡당 1회씩만 찍혔다 — 07-27 이중 실행 사고는 재발하지 않았다.

---

## 0. 요약

1. **안티에일리어싱은 전이 기준 null.** paired Δ(Target Test) = **+0.95 ± 4.44 %p** (se 1.99, n=5, t=0.48).
   애초에 이 실험의 목적은 전이 개선이 아니라 **우리 전처리가 만든 아티팩트를 닫는 것**이었고,
   그 목적은 달성됐다. 08-04 진단대로 EMG 도메인 갭은 에일리어싱이 아니라 samsung2 하드웨어
   고유의 대역 내 노이즈 바닥이었다. → **이후 실험은 AA on 을 기본값으로 간다.**
2. **Physics 보조손실은 방향은 +, 유의성은 없음.** 최고 조건 `rec0.3+jerk1.0` 이 paired
   **+1.75 ± 2.16 %p** (se 0.97, n=5, t=1.81). n=5 에서 유의 임계 t=2.776 을 아무 조건도
   못 넘는다. `rec1.0` 이 t=2.58 로 가장 근접.
3. **"물리 정보" 주장은 이 데이터로 성립하지 않는다.** 40회(8조건×5seed) 전체에서
   재구성 품질과 target 정확도의 상관이 **r = −0.07** 로 사실상 0이다. 가장 좋은 조건은
   jerk_ratio = 0.339 로 **과평활**인데도 1위다. 즉 이득의 정체는 물리적 타당성이 아니라
   **디코더 gradient 의 표현 정규화 효과**다. 논문에 쓸 때 표현을 반드시 낮춰야 한다.
4. **다만 target 배치에 거는 것 자체는 의미가 있다.** both − src만 = **+1.95 ± 2.88 %p**
   (se 1.29, t=1.51). src만 조건은 recon_corr 이 0.076 으로 붕괴한다(양쪽 0.619) —
   라벨 없는 target IMU 를 쓰는 게 UDA 기여로 성립할 여지는 남았다. 유의하진 않다.

---

## 1. 실험 1 — EMG 안티에일리어싱 A/B

### 1.1 무엇을 물었나

08-04 진단(`finding-emg-domain-gap-hf-noise`)에서 EMG A-distance ≈ 2.0 의 서명이 전부 고주파였다.
원인 후보 두 개 중 하나가 **우리 전처리의 에일리어싱**이었다: 네이티브 레이트에서 1000Hz 로
리샘플할 때 안티에일리어싱 LPF 없이 데시메이션하면 나이퀴스트 위 성분이 대역 안으로 접힌다.
접히는 양은 기기별 원본 레이트가 다르므로(samsung1 148Hz / samsung2 66Hz) 기기 지문이 된다.

구현: 데시메이션 **전** 네이티브 레이트에서 8차 Butterworth LPF, 컷오프 450Hz
(= 이후 밴드패스 상한과 동일). `data_preprocess_MM.py:146-148, 187`.
네이티브 레이트가 이미 2×450Hz 이하면 스킵한다(`:146`).

### 1.2 두 팔의 무결성

전처리를 두 번 돌려 **AA 만 다른** 두 데이터셋을 만들고 나머지가 동일함을 검사했다:

| 검사 | 결과 |
|---|---|
| 세션 분할 | 동일 — source [354, 77, 77] / target [338, 73, 73] |
| `preproc_config` 차이 | `emg_aa_cutoff`, `emg_antialias` 둘뿐 |
| IMU · 라벨 | 전 스플릿 완전 동일 |
| EMG 평균절대차 | source 0.0053~0.0059 / **target 0.0789~0.0853** |

target 쪽 EMG 차이가 source 의 **14배**다. 원본 레이트가 낮은 samsung2 에서 접힘이
훨씬 컸다는 뜻이고, 에일리어싱이 실재했다는 직접 증거다.

### 1.3 결과 (TEST 기준, selection = source val, leakage-free)

| arm | Src-Test | Tgt-Test | Test-Shift |
|---|---|---|---|
| AA off (대조) | 86.13 ± 1.25 | 26.07 ± 4.78 | 60.06 ± 3.58 |
| AA on 450Hz | 86.93 ± 1.08 | 27.02 ± 1.98 | 59.92 ± 2.07 |

paired Δ (같은 seed, AA on − AA off):

| 지표 | Δ | se | t (n=5) |
|---|---|---|---|
| Source Test | +0.81 ± 2.19 | 0.98 | +0.83 |
| Target Test | +0.95 ± 4.44 | 1.99 | +0.48 |
| Test Shift | −0.14 ± 2.58 | 1.15 | −0.13 |

seed별 Target Test — off `[32.03, 27.86, 27.25, 19.23, 23.96]` / on `[29.07, 24.87, 28.89, 27.07, 25.19]`

### 1.4 해석

- Source 가 거의 안 변했다(+0.81) — 정상이다. 450Hz 위는 원래 비어 있으니 LPF 가
  신호를 깎지 않았다는 확인이다.
- Target 이 +0.95 로 유의하지 않다. 진단에서 에일리어싱은 HF 격차의 15~26% 뿐이라고
  나왔으므로 예상 범위다. **"아티팩트는 닫았고 남은 갭은 하드웨어 고유"** 가 결론이다.
- 곁가지 — seed 표준편차가 4.78 → 1.98 로 줄었다. n=5 라 단정할 수 없지만, source-only
  EMG 전이가 접힌 잡음에 좌우돼 불안정했을 가능성이 있다. 재현할 값어치는 있다.

---

## 2. 실험 2 — Physics-informed CDAN

### 2.1 설계

```
L = L_cls + w_d·L_domain + λ_rec·L_rec + λ_jerk·L_jerk
```

EMG 인코더 표현에서 디코더가 3축 IMU 를 복원하도록 강제한다(`EMG_physics/physics_model.py`).
두 보조손실 다 **짝꿍 IMU 만 쓰고 라벨을 안 본다** → 기본적으로 target 배치에도 걸린다.
데이터는 `preprocessed_MM_pca` (재구성 타깃이 IMU 라 두 도메인이 같은 축 프레임이어야 함).

동치성 검증 — λ=0 일 때 기존 CDAN 과 class/domain 로짓 최대차 **0.00e+00** (`driver.log` Phase 0).
파라미터 11,566,092 → 12,134,415 (디코더 568,323 = +4.9%).

λ 범위는 초기화 직후 프로브로 잡았다: L_cls=2.31, L_rec=0.53, L_jerk=0.163
(정답 IMU jerk=0.0047 — 디코더 초기 출력이 34배 튄다).

### 2.2 결과 (TEST 기준, selection = target_val oracle — 기존 MM CDAN 과 동일 규약)

| 조건 | Src-Test | Tgt-Test | paired Δ | t (n=5) | recon_corr | jerk_ratio |
|---|---|---|---|---|---|---|
| 대조군 (λ=0) | 94.39 ± 1.21 | 88.37 ± 2.43 | — | — | −0.024 | 4.063 |
| rec 0.1 | 93.68 ± 1.66 | 88.91 ± 2.10 | +0.54 ± 2.54 | 0.48 | 0.617 | 1.014 |
| rec 0.3 | 93.40 ± 2.13 | 88.17 ± 2.68 | −0.19 ± 2.96 | −0.15 | 0.279 | 0.995 |
| **rec 1.0** | 93.99 ± 2.07 | 89.74 ± 2.18 | **+1.37 ± 1.19** | **2.58** | 0.734 | 0.837 |
| rec 0.3 + jerk 0.01 | 93.00 ± 3.12 | 87.69 ± 2.16 | −0.68 ± 2.15 | −0.71 | 0.459 | 0.897 |
| rec 0.3 + jerk 0.1 | 94.27 ± 0.96 | 89.94 ± 1.47 | +1.57 ± 3.43 | 1.02 | 0.619 | 0.679 |
| **rec 0.3 + jerk 1.0** | 93.43 ± 1.06 | **90.12 ± 1.75** | **+1.75 ± 2.16** | 1.81 | 0.541 | **0.339** |
| rec 0.3 + jerk 0.1 (src만) | 93.32 ± 1.10 | 87.99 ± 1.92 | −0.38 ± 1.29 | −0.66 | 0.076 | 0.759 |

Source Test 는 전 조건 −0.13 ~ −1.39 %p 로 거의 안 움직였다 — 보조손실이 분류 용량을
잡아먹지는 않았다.

### 2.3 세 가지 확인

**(a) 디코더는 실제로 학습됐다.** 대조군 recon_corr 이 −0.024 (디코더가 gradient 를 못 받아
난수 상태)인 반면 λ_rec>0 조건은 0.28~0.73. `rec1.0` 이 0.734 로 최고 — λ_rec 이 클수록
재구성이 좋아지는 단조 관계가 성립한다. 디코더가 붙긴 붙었다.

**(b) 그런데 재구성 품질이 정확도를 설명하지 못한다.** 40회 런 전체 pooled Pearson
**r(recon_corr, target_test_acc) = −0.070**. 부호까지 음수다. 재구성을 잘할수록 전이가
좋아진다는 관계가 없다.

**(c) 최고 조건은 과평활 상태다.** `rec0.3+jerk1.0` 의 jerk_ratio = 0.339 — 예측 jerk 가
실제의 1/3 로, 디코더가 물리적으로 맞는 신호를 내는 게 아니라 밋밋한 신호를 낸다.
그런데도 Target 1위다.

→ (a)+(b)+(c) 를 합치면 **이득의 정체는 "물리 정보"가 아니라 디코더 gradient 가 인코더에
거는 정규화**다. 러너 주석에 미리 적어 둔 경고가 그대로 관측됐다. 논문에서
"physics-informed" 로 파는 건 이 증거로는 무리고, "auxiliary kinematic reconstruction as
representation regularizer" 정도가 방어 가능한 표현이다.

### 2.4 target 보조손실의 순기여

both − src만 = **+1.95 ± 2.88 %p** (se 1.29, n=5, t=1.51).

숫자보다 의미 있는 건 recon_corr 이다 — src만 조건은 **0.076** 으로 사실상 0이다.
source 에서만 배운 디코더는 target EMG 에서 IMU 를 전혀 복원하지 못한다. 즉 재구성 능력
자체가 도메인을 안 넘는다. target 배치에 걸어야만 디코더가 양쪽에서 동작하고, 그 차이가
+1.95 로 나타난다. UDA 기여 주장의 여지는 여기에 있다. **다만 유의하진 않다.**

---

## 3. 통계 주의

- 단위는 **seed 5개**다. paired t 의 유의 임계는 두 꼬리 0.05 에서 **t = 2.776** 이고,
  이번 실험에서 이를 넘은 조건은 **없다**. `rec1.0` (2.58) 이 가장 근접.
- 윈도우 90% 중첩 → 명목 n 의 유효 표본수는 약 1/10, 세션 1개가 ≈ 1.37 %p.
  Δ 가 1%p 안쪽이면 유의하다고 읽으면 안 된다 (`gotcha-window-overlap-effective-n`).
- 실험 2 의 selection 은 target_val **oracle** 이다. 기존 MM CDAN 표와 맞추려고 그렇게
  했지만 절대수치를 leakage-free 로 주장할 수는 없다. 실험 1(EMG) 은 source val 선택이라
  이 문제가 없다 — 두 실험의 selection 규약이 다르니 표를 섞지 말 것.
- `results/EMG/emg_baseline_summary.txt` (85.91% → 29.71%) 는 2026-06-25 자 **구 2분할**이다.
  태그가 없어 파일명이 안 겹쳐 남아 있을 뿐, 위 표와 **비교 금지**.

---

## 4. 다음에 할 것

1. **AA on 을 기본값으로 승격.** 앞으로 만드는 전처리는 `--emg_antialias --emg_aa_cutoff 450`
   을 붙인다. 기존 `preprocessed_MM_*` 는 전부 AA off 라, 새 표를 만들 땐 재생성이 필요하다.
2. **physics 를 더 밀 거라면 seed 를 늘려야 한다.** 현재 효과크기(+1.4~1.8 %p)에 관측된
   seed 산포(σ≈2.2)면 n=5 로는 절대 유의가 안 나온다. 최소 n=10~15.
3. **λ_rec 을 위로 더 훑을 값어치가 있다.** rec 1.0 이 재구성·정확도 양쪽에서 가장 깨끗한
   조건이고 (t=2.58, corr=0.734, ratio=0.837, Source 하락 −0.40 로 최소) λ_rec ∈ {2, 3}
   이 아직 미탐색이다. jerk 는 과평활 쪽이라 우선순위가 낮다.
4. **`preprocessed_MM_pca_isotropic` 재실행.** zscore 는 축별 독립 정규화라 회전 구조와
   축간 크기비를 깨는데, 물리 재구성에는 이게 불리하다. `MM_DATA_DIR` 만 바꾸면 된다.
